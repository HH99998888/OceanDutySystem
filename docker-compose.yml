services:
  backend:
    build: ./backend
    ports:
      - "8081:8081"
    volumes:
      - ocean-data:/app/data
  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
volumes:
  ocean-data:
